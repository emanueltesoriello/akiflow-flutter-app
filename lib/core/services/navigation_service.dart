import 'package:flutter/material.dart';

class NavigationService {
  // Use it in order to get access to a global context
  static GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
} 
